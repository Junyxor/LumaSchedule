<script lang="ts">
  import { ChevronLeft, ChevronRight, Filter, Plus } from 'lucide-svelte'; import type { Course } from '../types'; export let courses: Course[];
  const days = [['周一','07'],['周二','08'],['周三','09'],['周四','10'],['周五','11'],['周六','12'],['周日','13']]; const sections = Array.from({ length: 12 }, (_, i) => i + 1);
</script>
<section class="page page-week"><header class="topbar week-topbar"><div><span class="eyebrow">2026–2027 第一学期</span><h1>第 1 周</h1><p>9月7日 – 9月13日</p></div><div class="week-controls glass-panel"><button><ChevronLeft size={18} /></button><span>本周</span><button><ChevronRight size={18} /></button></div><div class="top-actions"><button class="icon-button glass-panel"><Filter size={18} /></button><button class="primary-round"><Plus size={20} /></button></div></header>
<div class="week-board glass-panel"><div class="week-header"><div class="corner">节</div>{#each days as day, i}<div class:today={i === 0}><span>{day[0]}</span><b>{day[1]}</b></div>{/each}</div><div class="week-scroll"><div class="section-column">{#each sections as n}<div><b>{n}</b><span>{n <= 2 ? (n === 1 ? '08:30' : '09:20') : ''}</span></div>{/each}</div><div class="week-gridlines">{#each Array(7) as _}<div></div>{/each}</div>{#each courses as course}<button class="week-course {course.color}" style={`--day:${course.day};--start:${course.startSection};--span:${course.endSection - course.startSection + 1}`}><b>{course.name}</b><span>{course.room}</span><small>{course.start}–{course.end}</small></button>{/each}<div class="now-line" style="--now:6.35"><span>14:12</span><i></i></div></div></div></section>
